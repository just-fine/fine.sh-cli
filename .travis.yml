language: node_js

cache:
  directories:
  - "node_modules"

notifications:
  email:
    on_success: never

branches:
  only:
    - master

before_install: git fetch --depth=1000

install:
  - npm i

script:
  - /bin/bash build/lint-commits.sh
  - npm run build
